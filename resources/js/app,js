import { defineConfig } from 'vite';

export default defineConfig({
    build: {
        outDir: 'public/assets',
        emptyOutDir: true, // This is optional but good practice to clear the folder on each build
        manifest: true,
        rollupOptions: {
            input: 'resources/js/app.js',
        },
    },
});